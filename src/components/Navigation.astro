---

---

<head>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400..900&family=Roboto+Condensed:ital,wght@0,100..900;1,100..900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet" />
</head>

<header class="header">
  <nav>
    <!-- Logo -->
    <a href="index"><img src="../img/logo.png" alt="logo" class="logodesk" /></a>

    <!-- Menu Button -->

    <!-- Ikoner -->

    <input class="menu-btn" type="checkbox" id="menu-btn" />
    <!-- Menu Links -->
    <!-- <ul class="menu">
      <li><a href="produktliste">SMYKKER</a></li>
      <li><a href="tjenester">TJENESTER</a></li>
      <li><a href="om-os">OM OS</a></li>
    </ul> -->

    <ul class="main_menu">
      <li class="dropdown">
        <a href="produktliste">SMYKKER</a>
        <ul class="dropdown-menu">
          <li><a href="list_earrings">Øreringe</a></li>
          <li><a href="list_necklaces">Halskæder</a></li>
          <li><a href="list_rings">Ringe</a></li>
          <li><a href="list_bracelets">Armbånd</a></li>
        </ul>
      </li>
      <li><a href="tjenester">TJENESTER</a></li>
      <li><a href="om-os">OM OS</a></li>
    </ul>

    <div class="icon_menu">
      <a href="#"><img src="../img/search.svg" alt="search" /></a>
      <a href="#"><img src="../img/shoppingbag.svg" alt="shopping bag" /></a>

      <label for="menu-btn"><span class="navicon"></span></label>
    </div>
  </nav>
</header>

<style>
  * ---------------------------- HEADER / BURGERMENU MOBIL VERSION---------------------------- */
/* Header styling */

.html {
    /* beskytter mod at siden er bredere ens den tilgængelige skærmplads */
    overflow-x: hidden;
    overflow-y: auto;
  }

  .header {
    width: 100%;
  }

  nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 16px;
  }

  .logodesk {
    max-width: 35%;
    height: auto;
    /* padding-left: 10px;
    padding-top: 10px; */
  }

  .main_menu {
    display: none;
  }

  .icon_menu {
    display: flex;
    /* margin-left: 55%; */
    justify-content: space-between;
    flex-grow: 0.7;
  }
  .icon_menu a img,
  .icon_menu label {
    max-width: 32px;
    height: auto;

    /* padding-right: 12px;
    padding-top: 2px; */
  }

  .header ul {
    margin: 0;
    padding: 0;
    list-style: none;
    background-color: var(--primary);
  }
  /* Links styling */
  .header li a {
    font-family: orbitron;
    font-weight: 400;
    font-style: normal;
    background-color: #f9f2f2;
    color: #1b72d0;
    display: block;
    padding: 20px 20px;
    text-decoration: none;
  }
  /* Menu styling */
  .main_menu a:hover {
    font-family: "josefin-sans", sans-serif;
    font-weight: 600;
    font-style: normal;
    color: #e34a6f;
  }

  .header .main_menu {
    clear: both;
    max-height: 0;
    transition: max-height 0.2s ease-out;
  }
  /* Menu icon styling */

  .header .icon_menu {
    /* cursor: pointer; */
    display: flex;
    /* float: right; */
    position: relative;
    user-select: none;
  }

  .navicon {
    background: black;
    display: block;
    height: 2px;
    position: relative;
    transition: background 0.2s ease-out;
    width: 18px;
  }

  .navicon:before,
  .navicon:after {
    background: #000000;
    content: "";
    display: block;
    height: 100%;
    position: absolute;
    transition: all 0.2s ease-out;
    width: 100%;
  }

  .header .icon_menu .navicon:before {
    /* top: 5px; */
  }

  .header .icon_menu .navicon:after {
    /* top: -5px; */
  }

  .header .menu-btn {
    display: none;
  }

  .header .menu-btn:checked ~ .main_menu {
    display: block;
    position: fixed; /* positionerer efter hele skærmen */
    width: 100vw; /* VW fordi det knytter sig til hele skærmstørrelsen */
    left: 0;
    top: 88px;
  }
  /* funktion på de 3 streger til burgermenu */

  .header .menu-btn:checked ~ .icon_menu .navicon {
    background: transparent;
  }

  .header .menu-btn:checked ~ .icon_menu .navicon:before {
    transform: rotate(-45deg);
  }

  .header .menu-btn:checked ~ .icon_menu .navicon:after {
    transform: rotate(45deg);
  }

  .header .menu-btn:checked ~ .icon_menu:not(.steps) .navicon:before,
  .header .menu-btn:checked ~ .icon_menu:not(.steps) .navicon:after {
    top: 0;
  }

  /* ---------------------------- MENU DESKTOP---------------------------- */

  @media (min-width: 768px) {
    header {
      width: 100%;
      /* background-color: #f9f2f2; */
      padding: 5px;
      position: sticky;
      top: 0; /* Sørger for at headeren bliver siddende øverst */
      z-index: 100; /* Sørger for, at headeren er ovenpå andet indhold */
    }

    .logodesk {
      max-width: 100%;
      height: auto;
      padding-left: 20px;
      padding-top: 10px;
    }

    .icon_menu a img {
      max-width: 20%;
      height: auto;
      display: inline-block;
      /* float: right; */
      position: relative;
    }

    nav img {
      max-width: 40%;
    }

    a {
      text-decoration: none;
      color: black;
      font-family: orbitron;
      font-weight: 600;
      font-style: normal;
      font-size: 1rem;
      margin-bottom: 7px;
    }

    .streg1 {
      border: 2px solid var(--accent);
      margin-top: 1rem;
    }

    .streg2 {
      border: 1px solid var(--accent);
      margin-top: 0.3rem;
    }

    .header .menu {
      clear: none;
      max-height: none;
    }

    .icon_menu label {
      display: none;
    }

    /* Dropdown-menu styling */
    .dropdown-menu {
      position: absolute;
      top: 100%; /* Lige under hovedmenuen */
      left: 0;
      /* background-color: #fff; */
      /* Baggrundsfarve */
      border: 1px solid #ddd; /* Kant for dropdown */
      list-style: none;
      padding: 0;
      margin: 0;
      display: none; /* Skjult som standard */
      z-index: 10;
      min-width: 150px; /* Minimum bredde */
    }

    .dropdown-menu li {
      text-align: left;
    }

    .dropdown-menu a {
      padding: 10px 15px;
      color: #000;
      display: block;
    }

    .dropdown-menu a:hover {
      background-color: #f4f4f4; /* Hover-effekt */
      color: #e34a6f;
    }

    /* Vis dropdown ved hover */
    .main_menu .dropdown:hover .dropdown-menu {
      display: block; /* Gør dropdown synlig */
    }

    .main_menu {
      display: flex;
      justify-content: space-between;
      flex-grow: 2;
    }

    .main_menu .dropdown:hover .dropdown-menu {
      display: block;
      position: absolute;
      background-color: #f9f2f2;
      z-index: 1000;
      top: 58px;
    }
    .main_menu .dropdown {
      position: relative;
      /* z-index: 1000; */
    }
  }
</style>
